MongoDB хранит коллекции BSON документов вместо таблиц.
√еографические координаты записываютс€ в соответствии со стандартом GeoJSON.
ѕоле У_idФ генерируетс€ в базе данных автоматически, если не указано при загрузке документа.
Ѕаза данных joyIndex
 оллекци€ locations:
ƒокументы хран€т информацию об отдельных област€х на карте. —тандартный вид документа:
{
      У_idФ: ObjectId( _ ),				//ID области, может быть сгенерировано
      УtypeФ: УFeatureФ,				//“ребование стандарта GeoJSON	
      УgeometryФ: {					//”глы области-многоугольника на карте
      		УtypeФ: УPolygonФ,		//“ип фигуры, из стандарта
      		УcoordinatesФ: [ [Double, Double], Е ] // массив координат [долгота, широта]
       } 
}

ќсобое внимание к пор€дку координат: в стандарте сначала указываетс€ долгота (смещение запад-восток), потом широта (север-юг). ¬ –оссии прин€то располагать их наоборот, провер€йте, что загружаете.
ƒолгота находитс€ в рамках от 180 до -180 градусах, долгота: от -90 до 90.

 оллекци€ marks:
ƒокументы хран€т средние оценки за день с указанием даты и локации, которым они соответсвуют. —тандартный вид документа: 
{
	У_idФ: ObjectId( _ ),				//ID оценки, может быть сгенерировано
	УmarkФ: Double,				//—редн€€ оценка
	УdateФ: Date,					//ƒень оценки
	УcounterФ: Integer,				// оличество оценок
	УlocationФ: DBRef('locations', <locationDocumentName>._id )	//—оответствующа€ область из коллекции locations
}
